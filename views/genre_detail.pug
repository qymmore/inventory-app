extends layout

block content

  h1 Genre: #{genre.name}

  div(style='margin-left:20px;margin-top:20px')

    h4 Games

    dl
      each game in genre_games
        dt
          #{game.title}
        dd #{game.summary}

      else
        p This genre has no games
